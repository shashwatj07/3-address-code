SHASHWAT JAISWAL
11941090

1. Execute the shell file named run.sh (if on Windows, run the commands given in run.sh sequentially)
2. Execute the compiled executable with command line argument (c-file-path) using ./a.out c-file-path on Linux and a.exe c-file-path on Windows
3. The output will be displayed on the terminal.
4. 3AC.txt will have the generated 3 address codes
5. symbol_table.txt will have the symbols visible until the end of main function
